import Url from 'url';
import * as express from 'express';
import * as nock from 'nock';
import * as request from 'supertest';


describe('test', () => {
    const app = express();
    test('test', () => {
        app.get('/graphql')
    });
});
